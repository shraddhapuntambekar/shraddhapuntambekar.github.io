source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end

gem 'github-pages'

# Stdlib gems removed from Ruby's default set in Ruby 3.4+ / 4.0+.
# Required by the older Jekyll 3.9.x that github-pages pins.
gem 'csv'
gem 'base64'
gem 'bigdecimal'
gem 'logger'
gem 'fiddle'
